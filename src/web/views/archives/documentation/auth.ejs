<div class="doc-section">

<!-- ======================================================
  AUTH - LOGIN
  ========================================================= -->

  <div class="card card--doc">
    <p id="login">
      <span class="method">POST</span> <code>/api/auth/login</code> - Se connecter
    </p>

    <!-- ========== SUCCESS ========== -->
    <details>
      <summary>Réponses valides</summary>

      <p class="example-title">Exemple requête :</p>

<pre><code>{
    "email": "john@doe.com",
    "password": "Password123!"
}</code></pre>

      <p class="example-title">
        Réponse <span class="badge doc-badge--success">200 OK</span> :
      </p>

<pre><code>{
    "success": true,
    "message": "Connexion réussie",
    "accessToken": "jwt_token_string",
    "refreshToken": "jwt_token_string",
    "data": {
        "id": "...",
        "username": "jDoe",
        "email": "john@doe.com"
    }
}</code></pre>
    </details>

    <!-- ========== ERRORS ========== -->
    <details>
      <summary>Erreurs possibles</summary>

      <p class="example-title">
        Erreur <span class="badge doc-badge--error">400 Bad Request</span> :
      </p>

      <!-- CHAMP NON AUTORISÉ -->
      <p class="error-type">Champ(s) non autorisé(e)</p>

<pre><code>{
    "success": false,
    "message": "Champ(s) non autorisé(s) : nom_du_champ."
}</code></pre>

      <!-- CHAMPS REQUIS -->
      <p class="error-type">Email et mot de passe requis</p>

<pre><code>{
    "success": false
    "message": "Email et mot de passe requis."
}</code></pre>

      <p class="example-title">
        Erreur <span class="badge doc-badge--error">401 Unauthorized</span> :
      </p>

      <!-- CHAMPS INVALIDE -->
      <p class="error-type">Email ou mot de passe incorrect</p>

<pre><code>{
    "success": false
    "message": "Email ou mot de passe incorrect."
}</code></pre>
    </details>
  </div>

  <!-- ======================================================
    AUTH - REFRESH
    ========================================================= -->

  <div class="card card--doc">
    <p id="refresh">
      <span class="method">POST</span> <code>/api/auth/refresh</code> - Rafraîchir le token
    </p>

    <!-- ========== SUCCESS ========== -->
    <details>
      <summary>Réponses valides</summary>

      <p class="example-title">
        Réponse <span class="badge doc-badge--success">200 OK</span> :
      </p>

<pre><code>{
    "success": true,
    "message": "Token rafraîchi.",
    "accessToken": "jwt_token_string"
}</code></pre>
    </details>

    <!-- ========== ERRORS ========== -->
    <details>
      <summary>Erreurs possibles</summary>
      <p class="example-title">
        Erreur <span class="badge doc-badge--error">401 Unauthorized</span> :
      </p>

      <!-- CHAMPS INVALIDE -->
      <p class="error-type">Refresh token invalide ou expiré</p>

<pre><code>{
    "success": false
    "message": "Refresh token invalide ou expiré."
}</code></pre>
    </details>
  </div>

  <!-- ======================================================
    AUTH - LOGOUT
    ========================================================= -->

  <div class="card card--doc">
    <p id="logout">
      <span class="method">POST</span> <code>/api/auth/logout</code> - Se
      déconnecter
    </p>

    <!-- ========== SUCCESS ========== -->
    <details>
      <summary>Réponses valides</summary>

      <p class="example-title">
        Réponse <span class="badge doc-badge--success">200 OK</span> :
      </p>

<pre><code>{
    "success": true,
    "message": "Déconnecté avec succès."
}</code></pre>
    </details>
  </div>
</div>