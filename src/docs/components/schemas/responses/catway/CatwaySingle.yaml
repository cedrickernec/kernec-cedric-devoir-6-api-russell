# ==========================================================
# CATWAY SINGLE RESPONSE SCHEMA
# ----------------------------------------------------------
# Réponse standardisée pour la récupération
# d'un catway unique.
# ==========================================================

description: Structure standardisée des réponses de récupération d'un catway.
type: object
required: [success, data]

properties:
  success:
    type: boolean
    example: true

  data:
    $ref: "../../../../openapi.yaml#/components/schemas/CatwayModel"