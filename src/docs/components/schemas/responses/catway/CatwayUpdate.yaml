# ==========================================================
# CATWAY UPDATE RESPONSE SCHEMA
# ----------------------------------------------------------
# Réponse standardisée après mise à jour
# d'un catway.
# ==========================================================

description: Structure standardisée des réponses de mise à jour d'un catway.
type: object
required: [success, message, data]

properties:
  success:
    type: boolean
    example: true

  message:
    type: string
    example: Catway mis à jour.

  data:
    $ref: "../../../../openapi.yaml#/components/schemas/CatwayModel"