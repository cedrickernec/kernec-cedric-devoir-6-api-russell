# ==========================================================
# USER SINGLE RESPONSE SCHEMA
# ----------------------------------------------------------
# Réponse standardisée pour la récupération
# d'un utilisateur unique.
# ==========================================================

description: Structure standardisée des réponses de récupération d'un utilisateur.
type: object
required: [success, data]

properties:
  success:
    type: boolean
    example: true

  data:
    $ref: "../../../../openapi.yaml#/components/schemas/UserModel"