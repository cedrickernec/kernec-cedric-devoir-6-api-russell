# ==========================================================
# AUTH LOGIN REQUEST SCHEMA
# ----------------------------------------------------------
# Structure standardisée des requêtes de connexion
# utilisateur.
#
# Champs requis :
# - email
# - password
# ==========================================================

description: Structure standardisée des requêtes de connexion d'un utilisateur.
type: object
required: [email, password]

properties:
  email:
    description: Email utilisateur
    $ref: "../../../../openapi.yaml#/components/schemas/EmailRules"
    example: user@example.com

  password:
    description: Mot de passe utilisateur
    $ref: "../../../../openapi.yaml#/components/schemas/PasswordRules"
    example: MyPassword123!
