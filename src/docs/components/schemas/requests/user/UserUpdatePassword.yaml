# ==========================================================
# USER PASSWORD UPDATE REQUEST SCHEMA
# ----------------------------------------------------------
# Structure standardisée des requêtes de modification
# du mot de passe d’un utilisateur.
#
# Champs requis :
# - newPassword
# ==========================================================

description: Structure standardisée des requêtes de modification du mot de passe d'un utilisateur.
type: object
required: [newPassword]

properties:
  newPassword:
    description: Nouveau mot de passe utilisateur
    $ref: "../../../../openapi.yaml#/components/schemas/PasswordRules"