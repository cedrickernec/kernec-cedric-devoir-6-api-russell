# ==========================================================
# 404 NOT FOUND - ROUTE RESPONSE
# ----------------------------------------------------------
# Wrapper OpenAPI pour une route API inexistante.
#
# Référence le schéma NotFoundRoute.
# ==========================================================

description: Route introuvable
content:
  application/json:
    schema:
      $ref: "../../openapi.yaml#/components/schemas/NotFoundRoute"